# Deckord — pnpm settings
# Keep the workspace strict and reproducible.
link-workspace-packages=true
prefer-workspace-packages=true
# Allow importing directly from workspace package src/index.ts during dev.
shamefully-hoist=false
